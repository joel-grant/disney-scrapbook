name: Release
on:
  push:

jobs:
  release:
    uses: joel-grant/github-actions/.github/workflows/release-please.yaml@setup
    with:
      config-file: '.github/release-please-config.json'  # optional
      manifest-file: '.github/.release-please-manifest.json'  # optional
    secrets:
      release-token: ${{ secrets.MY_RELEASE_PLEASE_TOKEN }}
